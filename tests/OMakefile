.PHONY: testlib clean

OCAMLINCLUDES += ..

FILES = state_machine
PROGRAM = state_machine

OCAML_OTHER_LIBS += unix mltls
#OCAML_LIBS = mltls
OCAML_LINK_FLAGS = -linkall

# .DEFAULT: $(OCamlProgram $(PROGRAM), $(FILES))
OCamlProgram($(PROGRAM), $(FILES))

testlib: $(PROGRAM)$(EXE)

open ../../../config

clean:
   $(CLEAN) $(PROGRAM)$(EXE)
